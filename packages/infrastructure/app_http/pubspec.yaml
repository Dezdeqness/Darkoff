name: app_http
description: HTTP interceptors and helpers
publish_to: 'none'
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  dio: 5.10.0
  logger: 2.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
